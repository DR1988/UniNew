import React from 'react'
import MainComponent from './'

describe('MainComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
