import React from 'react'
import FormChoserComponent from './'

describe('FormChoserComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
