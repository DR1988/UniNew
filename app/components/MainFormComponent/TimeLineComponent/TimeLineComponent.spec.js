import React from 'react'
import TimeLineComponent from './'

describe('TimeLineComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
