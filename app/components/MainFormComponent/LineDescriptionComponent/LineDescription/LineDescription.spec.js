import React from 'react'
import LineDescription from './'

describe('LineDescription container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
