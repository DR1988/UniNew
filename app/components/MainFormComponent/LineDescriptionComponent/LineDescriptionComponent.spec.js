import React from 'react'
import LineDescriptionComponent from './'

describe('LineDescriptionComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
