import React from 'react'
import NoteComponent from './'

describe('NoteComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
