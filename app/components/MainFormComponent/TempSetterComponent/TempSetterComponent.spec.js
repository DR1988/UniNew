import React from 'react'
import TempSetterComponent from './'

describe('TempSetterComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
