import React from 'react'
import ValveLineComponent from './'

describe('ValveLineComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
