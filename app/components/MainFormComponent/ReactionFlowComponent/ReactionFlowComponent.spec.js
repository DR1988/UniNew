import React from 'react'
import ReactionFlowComponent from './'

describe('ReactionFlowComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
