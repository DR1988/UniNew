import React from 'react'
import LineFormerComponent from './'

describe('LineFormerComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
