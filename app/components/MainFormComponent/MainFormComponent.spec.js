import React from 'react'
import MainFormComponent from './'

describe('MainFormComponent container', () => {
  const props = {

  }


  it('should be properly tested', () => {
    expect(false).toBe(true)
  })
})
